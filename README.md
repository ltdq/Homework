# 学生信息管理系统

## 使用链表 + 哈希表实现查询O(1)，添加O(1)，删除O(1)；使用 Json 存储用户文件。

## 引入了 `rapidhash V3` 算法，或许是当前速度最快、碰撞率最低的不加密 Hash 算法
## 引入了 `yyjson 0.12.0` 处理 Json，也是极快的 Json 库

学校的作业

## TODO：
- [x] 数据持久化
- [ ] GUI
- [ ] 操作回退功能
- [ ] 最近访问记录/预测最想访问（预计使用类似 ARC 的算法）

### WIP
